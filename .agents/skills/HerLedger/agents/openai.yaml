interface:
  display_name: "HerLedger"
  short_description: "Repo-specific patterns and workflows for HerLedger"
  default_prompt: "Use the HerLedger repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true